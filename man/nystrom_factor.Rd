% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/approx-nystrom.R
\name{nystrom_factor}
\alias{nystrom_factor}
\title{Nystrom Low-Rank Factor for a Kernel}
\usage{
nystrom_factor(x, kernel = kernel_spec(), m = NULL, seed = NULL, tol = 1e-10)
}
\arguments{
\item{x}{Numeric matrix (n x d).}

\item{kernel}{A \code{kernel_spec} object. Bandwidth is resolved against
\code{x} if it is \code{"median"}.}

\item{m}{Integer landmark count. If \code{NULL}, defaults to
\code{\link[=default_nystrom_rank]{default_nystrom_rank()}}.}

\item{seed}{Optional integer for reproducible landmark sampling.}

\item{tol}{Eigenvalue threshold (relative to \code{max(eigval(W))}).
Eigenvalues at or below \code{tol * max(eigval(W))} are dropped, so the
returned factor may have fewer than \code{m} columns.}
}
\value{
A list with class \code{"nystrom_factor"}:
\itemize{
\item \code{U}: n x r factor matrix.
\item \code{landmarks}: integer vector of length m (sampled indices).
\item \code{m}: requested landmark count.
\item \code{rank}: effective rank \code{r} (columns of \code{U}).
\item \code{kernel}: the kernel spec used (with bandwidth resolved).
}
}
\description{
Computes the Nystrom factor \code{U} such that the approximation
\eqn{\tilde K = U U^\top} is a rank-r (with \eqn{r \le m}) approximation
of the exact kernel matrix \code{K = kernel_matrix(x, x, kernel)}.
}
\details{
This is the cheap representation: storing \code{U} (n x r) takes \code{O(nr)}
memory, and downstream HSIC/MMD computations can exploit the
factorisation in \verb{O(n r^2)} time rather than \code{O(n^2)}. Reconstructing
\code{tilde_K = U \%*\% t(U)} collapses back to an \verb{n x n} matrix and costs
\verb{O(n^2 r)}.
}
\examples{
set.seed(1)
x <- matrix(rnorm(200), 100, 2)
nf <- nystrom_factor(x, kernel_spec("rbf", bandwidth = 1), m = 30, seed = 42)
dim(nf$U)              # 100 x rank
K_hat <- nf$U \%*\% t(nf$U)
isSymmetric(K_hat)

}
\seealso{
\code{\link[=rff_features]{rff_features()}} for the Random Fourier Features alternative,
\code{\link[=kernel_spec]{kernel_spec()}} for opt-in via the \code{approx} argument.

Other approximations: 
\code{\link{rff_features}()}
}
\concept{approximations}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test-drdett.R
\name{dr_dett_test}
\alias{dr_dett_test}
\title{Doubly Robust Distributional Effect on the Treated Test (DR-DETT)}
\usage{
dr_dett_test(
  y,
  treatment,
  covariates,
  kernel_y = kernel_spec(),
  propensity_model = c("logistic", "ranger", "xgboost"),
  outcome_model = c("krr", "zero"),
  n_permutations = 500L,
  n_bins = 10L,
  regularisation = "cv",
  alpha = 0.05,
  adaptive = FALSE,
  B_max = 9999L,
  batch_size = 100L,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{y}{Numeric vector or matrix. Outcome variable.}

\item{treatment}{Binary vector (0/1). Treatment indicator.}

\item{covariates}{Numeric matrix, data.frame, or data.table. Confounders.}

\item{kernel_y}{Kernel specification for outcome space. Default is RBF.}

\item{propensity_model}{Character. Method for propensity estimation:
\code{"logistic"} (default), \code{"ranger"}, or \code{"xgboost"}.}

\item{outcome_model}{Character. \code{"krr"} (kernel ridge regression,
default) for the conditional mean embedding, or \code{"zero"} to use
only inverse probability weighting (no outcome regression).}

\item{n_permutations}{Integer. Number of permutations when
\code{adaptive = FALSE}. Default is 500.}

\item{n_bins}{Integer. Propensity score bins for permutation.
Default is 10.}

\item{regularisation}{Numeric or \code{"cv"}. Ridge parameter for the CME.
Default is \code{"cv"}.}

\item{alpha}{Numeric. Significance level. Used both as the reporting
threshold and, when \code{adaptive = TRUE}, as the early-stopping
threshold. Default is 0.05.}

\item{adaptive}{Logical. If \code{TRUE}, use the sequential / adaptive
permutation procedure of Besag & Clifford (1991) and Gandy (2009)
via \code{\link[=adaptive_permutation_pvalue]{adaptive_permutation_pvalue()}}. Default is \code{FALSE}
(byte-identical to previous behaviour).}

\item{B_max}{Integer. Hard upper bound on permutations when
\code{adaptive = TRUE}. Default is 9999.}

\item{batch_size}{Integer. Permutations drawn per adaptive batch.
Default is 100.}

\item{seed}{Integer or \code{NULL}. Random seed.}

\item{verbose}{Logical. Print progress. Default is \code{FALSE}.}
}
\value{
An object of class \code{"kernel_test_result"}.
}
\description{
Tests whether the distribution of the treated potential outcome Y(1)
differs from the control potential outcome Y(0) among the treated
subpopulation. Requires only one-sided overlap.
}
\details{
DR-DETT is analogous to DR-DATE but focuses on the \strong{effect on the
treated} (ETT) rather than the average treatment effect (ATE). It
requires only one-sided overlap: P(T=1|X) > epsilon (not bounded away
from 1), making it applicable in settings where positivity is
violated for the control group.
}
\examples{
set.seed(42)
n <- 300
x <- matrix(rnorm(n * 2), n, 2)
t <- rbinom(n, 1, plogis(0.5 * x[, 1]))
y <- t * rnorm(n, sd = 2) + (1 - t) * rnorm(n, sd = 1) + x[, 1]

result <- dr_dett_test(y, t, x, n_permutations = 200, seed = 1)
print(result)

}
\references{
Fawkes, J., Hu, R., Evans, R. J., & Sejdinovic, D. (2024). Doubly
robust kernel statistics for testing distributional treatment effects.
\emph{Transactions on Machine Learning Research}.
}
\seealso{
\code{\link[=dr_date_test]{dr_date_test()}} (average treatment effect distribution),
\code{\link[=kernel_causal_test]{kernel_causal_test()}} (formula interface).

Other tests-causal: 
\code{\link{bd_hsic_test}()},
\code{\link{dr_date_test}()},
\code{\link{kernel_causal_test}()}
}
\concept{tests-causal}

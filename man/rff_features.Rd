% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/approx-rff.R
\name{rff_features}
\alias{rff_features}
\title{Random Fourier Feature Map}
\usage{
rff_features(x, kernel = kernel_spec("rbf"), D = NULL, seed = NULL)
}
\arguments{
\item{x}{Numeric matrix (n x d).}

\item{kernel}{A \code{kernel_spec} object. The kernel \code{type} must be
\code{"rbf"} or \code{"matern"}. \code{bandwidth} is resolved against \code{x} if it is
\code{"median"}.}

\item{D}{Integer number of frequencies. The resulting feature matrix
has \code{2 * D} columns. If \code{NULL}, defaults to \code{\link[=default_rff_rank]{default_rff_rank()}}.}

\item{seed}{Optional integer for reproducible frequency sampling.}
}
\value{
A list with class \code{"rff_features"}:
\itemize{
\item \code{Z}: n x (2 * D) feature matrix.
\item \code{frequencies}: D x d matrix of sampled frequencies.
\item \code{D}: number of frequencies.
\item \code{kernel}: the kernel spec used (with bandwidth resolved).
}
}
\description{
Computes the RFF feature map \code{Z} such that the rank-\code{(2 * D)}
approximation of the exact kernel matrix is \eqn{\tilde K = Z Z^\top}.
Storing \code{Z} (n x 2D) takes \verb{O(n D)} memory and downstream computations
such as HSIC and MMD can in principle exploit the factorisation in
\verb{O(n D^2)} time rather than \code{O(n^2)}.
}
\details{
Bochner's theorem guarantees that for translation-invariant kernels
the spectral measure is non-negative; sampling \code{D} frequencies from it
and forming \verb{(cos, sin)} features gives an unbiased Monte Carlo
estimator of the kernel.
}
\examples{
set.seed(1)
x <- matrix(rnorm(200), 100, 2)
rf <- rff_features(x, kernel_spec("rbf", bandwidth = 1), D = 200, seed = 42)
dim(rf$Z)               # 100 x 400
K_hat <- rf$Z \%*\% t(rf$Z)
isSymmetric(K_hat)

}
\seealso{
\code{\link[=nystrom_factor]{nystrom_factor()}} for the Nystrom alternative, \code{\link[=kernel_spec]{kernel_spec()}}
for opt-in via the \code{approx} argument.

Other approximations: 
\code{\link{nystrom_factor}()}
}
\concept{approximations}

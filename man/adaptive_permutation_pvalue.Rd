% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation.R
\name{adaptive_permutation_pvalue}
\alias{adaptive_permutation_pvalue}
\title{Adaptive Sequential Permutation P-Value}
\usage{
adaptive_permutation_pvalue(
  observed,
  perm_fun,
  B_min = 99L,
  B_max = 9999L,
  batch_size = 100L,
  alpha = 0.05,
  wilson_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{observed}{Numeric scalar. The observed test statistic.}

\item{perm_fun}{A function \verb{function(n_perms)} returning a numeric
vector of \code{n_perms} permuted statistics drawn under the null. The
helper is test-agnostic: \code{perm_fun} encapsulates the sampling.}

\item{B_min}{Integer. Minimum number of permutations before stopping
is considered. Default 99.}

\item{B_max}{Integer. Hard upper bound on the number of permutations.
Default 9999.}

\item{batch_size}{Integer. Permutations drawn per batch. Default 100.}

\item{alpha}{Numeric in (0, 1). Significance level used to decide
early stopping. Default 0.05.}

\item{wilson_level}{Numeric in (0, 1). Confidence level for the Wilson
interval used to bound \code{p_hat}. Default 0.95.}

\item{seed}{Optional integer seed. If supplied, \code{set.seed(seed)} is
called before any permutation is drawn, providing reproducibility
across the entire adaptive procedure.}
}
\value{
A list with elements:
\describe{
\item{p_value}{Final permutation p-value with the standard +1
correction.}
\item{n_perms_used}{Total permutations drawn.}
\item{stop_reason}{One of \code{"upper_below_alpha"},
\code{"lower_above_alpha"}, or \code{"max_reached"}.}
\item{null_distribution}{Numeric vector of length \code{n_perms_used}
containing every permuted statistic drawn.}
}
}
\description{
Implements the sequential / adaptive permutation procedure of Besag &
Clifford (1991) and Gandy (2009): instead of running a fixed number of
permutations, draw permuted statistics in batches and stop early when
the resampled p-value's Wilson confidence interval clearly excludes
the significance level \code{alpha}. Type-I error is controlled by
construction.
}
\details{
At each step the running p-value is computed as
\code{p_hat = (1 + n_extreme) / (1 + n_total_perms)} and a Wilson
confidence interval at level \code{wilson_level} is built around \code{p_hat}
using \code{n_total_perms} as the sample size. The procedure stops when
(a) the upper bound is below \code{alpha}, (b) the lower bound is above
\code{alpha}, or (c) \code{n_total_perms} reaches \code{B_max}. The first batch is
sized \code{max(batch_size, B_min)} so that no decision is made before
\code{B_min} permutations have been drawn.
}
\references{
Besag, J., & Clifford, P. (1991). Sequential Monte Carlo p-values.
\emph{Biometrika}, 78(2), 301-304.

Gandy, A. (2009). Sequential implementation of Monte Carlo tests with
uniformly bounded resampling risk. \emph{Journal of the American
Statistical Association}, 104(488), 1504-1511.
}
\keyword{internal}

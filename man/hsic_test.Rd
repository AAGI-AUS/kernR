% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test-hsic.R
\name{hsic_test}
\alias{hsic_test}
\title{HSIC Independence Test}
\usage{
hsic_test(
  x,
  y,
  kernel_x = kernel_spec(),
  kernel_y = kernel_spec(),
  n_permutations = 500L,
  alpha = 0.05,
  adaptive = FALSE,
  B_max = 9999L,
  batch_size = 100L,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric vector, matrix, or data.frame. First variable.}

\item{y}{Numeric vector, matrix, or data.frame. Second variable.}

\item{kernel_x}{Kernel specification for \code{x}. Default is RBF with
median heuristic.}

\item{kernel_y}{Kernel specification for \code{y}. Default is RBF with
median heuristic.}

\item{n_permutations}{Integer. Number of permutations for the null
distribution when \code{adaptive = FALSE}. Default is 500.}

\item{alpha}{Numeric. Significance level. Used both as the reporting
threshold and, when \code{adaptive = TRUE}, as the early-stopping
threshold. Default is 0.05.}

\item{adaptive}{Logical. If \code{TRUE}, use the sequential / adaptive
permutation procedure of Besag & Clifford (1991) and Gandy (2009)
via \code{\link[=adaptive_permutation_pvalue]{adaptive_permutation_pvalue()}}. Default is \code{FALSE}
(byte-identical to previous behaviour).}

\item{B_max}{Integer. Hard upper bound on permutations when
\code{adaptive = TRUE}. Default is 9999.}

\item{batch_size}{Integer. Permutations drawn per adaptive batch.
Default is 100.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}
}
\value{
An object of class \code{"kernel_test_result"} with components:
\describe{
\item{statistic}{The observed HSIC test statistic.}
\item{p_value}{Permutation p-value.}
\item{method}{\code{"HSIC"}.}
\item{n}{Sample size.}
\item{n_permutations}{Number of permutations used.}
\item{null_distribution}{Vector of permuted HSIC values.}
\item{kernel_x, kernel_y}{Kernel specifications used (with
resolved bandwidths).}
\item{call}{The matched call.}
}
}
\description{
Tests whether two variables are independent using the Hilbert-Schmidt
Independence Criterion (HSIC). Uses a permutation test for inference.
}
\examples{
set.seed(42)
n <- 200
x <- rnorm(n)

# Dependent case
y_dep <- x^2 + rnorm(n, sd = 0.5)
result <- hsic_test(x, y_dep)
print(result)

# Independent case
y_ind <- rnorm(n)
result <- hsic_test(x, y_ind)
print(result)

}
\references{
Gretton, A., Fukumizu, K., Teo, C. H., Song, L., Scholkopf, B., &
Smola, A. J. (2008). A kernel statistical test of independence.
\emph{NeurIPS}, 20.
}
\seealso{
\code{\link[=mmd_test]{mmd_test()}} for two-sample testing; \code{\link[=bd_hsic_test]{bd_hsic_test()}} for the
causal-association extension.

Other tests-base: 
\code{\link{mmd_test}()}
}
\concept{tests-base}
